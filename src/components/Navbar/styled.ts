import styled from "styled-components";

const NavbarStyledWrapper = styled.div`
  margin-top: 5px;
  display: flex;
  justify-content: space-between;

  a {
    cursor: pointer;
    margin: 0 10px;
    text-decoration: none;
    color: #708090;
  }  
  
  .portfolio-routes {
    display: flex;
    justify-content: left;
    margin-left: 15%;
  }

  .page-routes {
    display: flex;
    justify-content: right;
    margin-right: 15%;
  }
  .
`;

export default NavbarStyledWrapper;
